字段内容里含分隔符的导入导出：正常导入导出，源文本字段中分隔符需要转义，比如："a""b"为合法

字段含回车符的导入导出：支持文本跨行

源表有键值，导入文件引起键值冲突问题：采用replace方式替换，不报错

源表有键值，导入文件自身对键值有冲突问题：采用replace方式，取最后一条，不报错

根据分隔符字段不足或多余的容错报警：支持

NULL值如果是\N的如何导入问题：使用escape="\\\\"



还缺：
	FTP或SFTP工具
	MYSQL库与库同步工具


